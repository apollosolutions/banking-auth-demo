{
  "name": "@apollosolutions/router-playground-coprocessor",
  "version": "1.0.0",
  "description": "A coprocessor example for the router",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=dev nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "nodemonConfig": {
    "ext": "js,json"
  }
}
